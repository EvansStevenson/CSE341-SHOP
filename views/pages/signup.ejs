<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up</title>
  <link rel="stylesheet" href="/css/home.css">
  <style>
    li a.home {
      background-color: rgb(74, 74, 211);
      color: white;
    }
  </style>
  <%- include('../includes/head.ejs')%>
</head>

<body>
  <%- include('../includes/nav.ejs', {active: "active"}, {notActive: ""}) %>

  <div style="margin-left:25%;padding:1px 16px;height:1000px;">
    <form action="/auth/signup/submit" method="POST" novalidate>
      <div class="form-group mt-3">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">

        <label class="mb-3" for="email">Email</label>
        <input class="<%= errorMessages.find(e => e.param === 'email') ? 'invalid' : '' %>" type="email" name="email"
          id="email" value="<%= oldEmail %>"><br>

        <label class="mb-3" for="password">Password</label>
        <input class="<%= errorMessages.find(e => e.param === 'password') ? 'invalid' : '' %>" type="password"
          name="password" id="password"><br>

        <label class="mb-3" for="password">Confirm Password</label>
        <input class="<%= errorMessages.find(e => e.param === 'confirmPassword') ? 'invalid' : '' %>" type="password"
          name="confirmPassword" id="confirmPassword"><br>

        <button class="btn btn-info" type="submit">Signup</button>
      </div>
    </form>
    <% errorMessages.forEach(err => { %>
    <p style="color: red;">*<%= err.msg %> </p>
    <% }) %>
  </div>
</body> 

</html>