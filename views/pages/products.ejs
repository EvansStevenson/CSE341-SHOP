<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Products</title>
    <link rel="stylesheet" href="/css/home.css">
    <style>
        li a.shop {
            background-color: rgb(74, 74, 211);
            color: white;
        }
    </style>

</head>

<body>
    <%- include('../includes/nav.ejs', {active: "active"}, {notActive: ""}) %>

    <div style="margin-left:25%;padding:1px 16px;height:1000px;">
        <% prods.forEach (prods => { %>
        <a href="../shop/<%= prods._id %>" style="text-decoration: none;">
            <div>
                <h1 class="display-4"><%= prods.title %></h1>
                <img src="<%= prods.imgUrl %>" style='max-width: 150px'>
                <p>$<%= prods.price %></p>
                <% if (isAuth){ %>
                <%- include('../includes/addToCart.ejs', {product: prods._id}) %>
                <% } %>

            </div>
        </a>
        <% }) %>
    </div>

</body>

</html>